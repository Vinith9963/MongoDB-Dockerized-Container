# MongoDB-Dockerized-Container

Docker container creating a containerized Mongodb environment. Showing all the commands that are required for any naïve user to understand the basics of Mongodb.

command 1 - 10 shows how to initialize the mongodb environment in docker container along with uploading the user data in the database.
